## Why
- 当前项目缺少单元测试框架，难以及时发现计费/授权等关键逻辑的回归。
- 用户希望增加 Vitest 以便对服务端核心逻辑做单元测试。

## What Changes
- 引入 Vitest 作为单元测试框架，新增 `pnpm test` 入口。
- 增加基础测试配置（TypeScript/ESM 兼容）。
- 为服务端核心模块补充最小集单元测试（以计费/订阅映射为首批覆盖）。

## Impact
- 仅影响开发/测试流程，不改变运行时行为。
- 新增开发依赖与测试脚本。
- 初始测试覆盖范围有限，后续可逐步扩展。
